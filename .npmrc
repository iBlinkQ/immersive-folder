# npm tags a version commit as v1.2.3 by default. The release workflow
# compares the tag name against manifest.json's version verbatim, and every
# tag this repository has published is bare — so the default prefix would
# fail the check and publish nothing. Empty it here rather than remembering
# to pass --no-git-tag-version at each release.
tag-version-prefix=""
