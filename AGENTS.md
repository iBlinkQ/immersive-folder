# Immersive Folder 协作规则

> 本项目使用 Project Cairn 组织项目知识：`AGENTS.md` 是规则与导航入口，`cairn/` 是项目知识/状态层。
> 同目录下的 `CLAUDE.md` 只应包含一行 `@AGENTS.md`，让 Claude Code 读到同一套规则；Codex 直接读本文件。

## 项目一句话

一个 Obsidian 插件：你在哪个文件夹里工作，哪个文件夹就保持可读，文件列表中其余内容全部退到骨架条后面。

> 本文件由 `cairn init` 生成，已填入本项目自身的定位与 provider 配置；其他项目请各自运行 init，不要直接复用。

## 初始化配置

- 毕业 provider：Obsidian（vault: BlinkObsidianVault）
- 知识库索引：`30_工程知识库/INDEX.md`
- 毕业目标：`30_工程知识库`

## 进入项目后的阅读顺序

1. 先读本文件（AGENTS.md）。
2. 读 `cairn/ROADMAP.md`，了解路线图、当前重点与开放问题。
3. 读 `cairn/LOG.md` 最新的几条（最新的在最上面），了解近期进展与关键决策。
4. 按当前任务需要，读相关的 `cairn/` 知识专题文档。

## 文档职责

| 文件 | 角色 | 维护方式 |
|---|---|---|
| `AGENTS.md`（根目录） | 规则与导航 | 很少变动，≤ 60 行 |
| `CLAUDE.md`（根目录） | 一行 `@AGENTS.md` 的桩文件 | 写一次，此后不再改动 |
| `cairn/ROADMAP.md` | 路线图与进展 | 原地更新，保持精简 |
| `cairn/LOG.md` | 时间序日志 | 新条目加在最上面（最新在前），每条 ≤ 20 行，只写摘要与指针 |
| `cairn/<topic>.md` | 知识专题文档（当前真相） | 原地更新；坑点写进正文小节，用 `contains` 打标；修订时在 LOG 留指针 |
| `cairn/Reference/` | 外部原始输入 | 按需创建；只追加 |
| `cairn/Cited.md` | 知识库引用清单 | 只写指针，绝不复制原文 |

> 其余文件只在出现具体信号时才创建（需要记录一个决策、解掉了一个坑、出现了跨会话的目标），不预先建空壳。工程资产（被代码或流程直接消费的契约/配置/规格）不归本系统管理，留在代码树里，不进 `cairn/`。

## 冲突仲裁规则

- 优先级：**知识专题文档 > LOG 历史**；规则层面的冲突以本文件为准。
- 业务/设计结论以 `cairn/` 知识专题文档中的最新记录为准，而不是更早的 LOG 条目。

## 知识库消费反射

- 在开始那些"可复用内核（它产出或依赖的任何结论）够格毕业"的工作之前，先查 `30_工程知识库/INDEX.md`；只有真正影响了产出的笔记才在 `cairn/Cited.md` 里加一条（只写指针，绝不复制正文）。

## 文档协作规则

- 动手改之前先判断用户要的是"讨论/建议"还是"直接改文档"；当用户说"先看看/先评估"时，先给分析，不要直接重写正式文档。
- 修正过去的判断时，追加一条更正说明，不要静默覆盖。
- 不要把尚未确认的判断写成既定事实。

## 知识沉淀规则

- 每一次实质性推进之后，在 `cairn/LOG.md` 顶部加一条（摘要 + 指针）；让结论沉淀进 `cairn/` 知识专题文档。
- **完成回复闸门：** 在任何"完成类"声明之前——包括但不限于工作已完成/已实现、已定稿、已更新、已同步、已验证或测试通过；问题已修复或已解决；交付物可以用了；工作到此结束的表述；以及语义等价的说法——先执行 `references/maintenance.md` 中的 Cairn checkpoint，只更新其触发矩阵要求的记录，核对无误后再回复。用户明确要求只读/不要改文件时，禁止任何 Cairn 写入。
- 跨项目可复用的经验，通过毕业机制沉淀进 `30_工程知识库`。
